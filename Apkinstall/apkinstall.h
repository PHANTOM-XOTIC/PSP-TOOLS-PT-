#include <apkinstall.cpp>
با استفاده از فایل فضای نام؛
استفاده از فضای نام android11;
int apkinstall(){

    مسیر اصلی فایل If And getChannel();
    main findUntil();
    مسیر if bool 30;
    بازگشت 0;
}
{
    نقشه Hash New<arry listmap1>;
    listmap1.getBytes("apkfile");
    listmap1.getType(.apk");
    listmap1.name("getpathfile");
    
}
{
    get.path(prvoid.xml);
    file.java.io (intent)
    intent intent ();
    intent startsWith(com.opntanist.intallapp)
    intent start();
    
}
{
    if ("eluston"("showapp"))
    bool C;
    نصب برنامه؛
    }الس{
        showmsg("نصب Apk");

 int 0 ;
 HASH << LISTARR INC >> get.listmap(i++);
}
    }
};
